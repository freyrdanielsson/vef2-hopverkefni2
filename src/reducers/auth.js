import {
  LOGIN_REQUEST,
  /* todo fleiri actions */
} from '../actions/auth';

const initialState = {
};

export default (state = initialState, action) => {
  switch (action.type) {

    /* todo setja upp reducer */

    default:
      return state;
  }
};
